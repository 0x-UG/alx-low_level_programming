#include <stdio.h>

/**
 * main - prints all the single digit numbers of base 10
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	for (i = 58; i < 68; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
